{
  "name": "sheets-web-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file sheets viewer (static, no build step). This package.json exists only for the CI harness tooling.",
  "scripts": {
    "test": "node test/harness.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
